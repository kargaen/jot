# Store

This is a future home for shared client state if the app settles on an explicit store layer.

Possible future use:
- Task slice
- Space slice
- Project slice
- UI state slice

Only introduce a store if it clarifies controller boundaries instead of bypassing them.
