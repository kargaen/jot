# Presentation Utils

Presentation-oriented helpers such as colors, icons, and theme helpers live here.

When wiring happens:
- Update imports from the old `src/lib/colors.ts`, `icons.ts`, and `theme.ts` paths.
- If design tokens or theming grow substantially, split this folder into clearer subdomains.
