# Task Models

Pure task-domain transforms and task-shape helpers belong here.

Current note:
- This folder already contains the first extracted task-domain helpers.

When wiring happens:
- Keep these files free of React, Supabase client calls, and window APIs.
- Expand this folder with task schemas, sorting rules, and task-specific view models as the rest of the app is cleaned up.
