# Pulse Desktop View

This folder contains the desktop reminder and daily-focus window.

When wiring happens:
- Update imports from the old `ReminderWindow.tsx` path.
- Consider separating reminder scheduling, monitor placement, and pulse presentation into distinct layers.
