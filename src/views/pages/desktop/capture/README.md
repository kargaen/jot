# Capture Desktop View

This folder contains the desktop quick-capture surface.

When wiring happens:
- Update imports from the old `QuickCapture.tsx` path.
- This folder is a likely candidate for future extraction of shared capture components.
