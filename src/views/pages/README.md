# Page Views

Route-level, window-level, or app-surface views belong here.

Current direction:
- Desktop windows are grouped under `desktop`.
- Mobile entry views are grouped under `mobile`.

When wiring happens:
- Consider whether some of these surfaces should eventually move behind a router or a window registry.
