# Mobile Page Views

Mobile-first app surfaces live here.

Current note:
- `MobileApp.view.tsx` was moved here so the tree clearly shows mobile as a view surface, not a parallel app.

Future idea:
- If mobile-specific overrides grow, this folder can branch into tabs, widgets, and device-specific surfaces without mixing them into desktop folders.
