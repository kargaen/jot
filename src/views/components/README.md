# View Components

Reusable UI pieces live here.

Current direction:
- `ui` holds generic atoms.
- Domain folders such as `tasks` and `pulse` hold reusable domain-specific views.

When wiring happens:
- Rename or split files further if the task views stay large.
