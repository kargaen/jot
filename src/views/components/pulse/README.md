# Pulse View Components

Pulse and daily-focus view pieces belong here.

Current note:
- `CompletionHeatmap.view.tsx` was moved here because it is a presentation concern, not a model or service.

Future idea:
- A dedicated `PulsePanel.view.tsx` and `PulseItem.view.tsx` would fit naturally here once the reminder and daily-focus surfaces are decomposed.
