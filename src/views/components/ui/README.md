# UI View Components

Generic UI primitives belong here.

Current note:
- `Toggle.view.tsx` is the first moved atom.

When wiring happens:
- Keep this folder domain-agnostic.
- If more atoms arrive, add barrel exports and a clearer design-system convention.
