<?xml version="1.0" encoding="utf-8"?>
<!-- Destination: src-tauri/gen/android/app/src/main/res/layout/widget_quick_capture.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quick Capture"
        android:textColor="#FF655BE8"
        android:textSize="11sp"
        android:textStyle="bold"
        android:letterSpacing="0.06" />

    <TextView
        android:id="@+id/capture_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:background="@drawable/capture_field_bg"
        android:gravity="center_vertical"
        android:minHeight="46dp"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="14dp"
        android:paddingBottom="10dp"
        android:text="Tap to type a thought"
        android:textColor="#FF171717"
        android:textSize="15sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Open capture or dictate instantly"
            android:textColor="#FF6B7280"
            android:textSize="11sp" />

        <ImageButton
            android:id="@+id/capture_button"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/capture_button_bg"
            android:contentDescription="Voice capture"
            android:scaleType="center"
            android:src="@drawable/ic_mic"
            android:tint="#FFFFFFFF" />
    </LinearLayout>

</LinearLayout>
