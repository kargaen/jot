# Generated automatically by `npm run db:env:e2e`.
# You usually do not need to edit this by hand.
#
# The real working file is `.env.e2e.local`, which is ignored by git.
# It is populated from `npx supabase status -o env` after the local stack starts.
